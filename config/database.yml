development:
  adapter: sqlite
  database: dev.db

test:
  adapter: sqlite
  database: test.db

development_mysql:
  adapter: mysql2
  encoding: utf8
  charset: utf8
  database: support_development
  username: root
  password: root
  socket: /tmp/mysql.sock

test_mysql:
  adapter: mysql2
  encoding: utf8
  charset: utf8
  database: support_test
  username: root
  password:
  socket: /tmp/mysql.sock
